[package]
name = "sparse-solver-wasm"
version = "0.1.0"
edition = "2024"

[dependencies]
wasm-bindgen = "0.2"
faer = { version = "0.24", default-features = false, features = ["sparse-linalg"] }

[lib]
crate-type = ["cdylib"]
